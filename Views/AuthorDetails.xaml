<Window x:Class="SoftwareDevelopment1.Views.AuthorDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SoftwareDevelopment1.Views"
        mc:Ignorable="d"
        Title="AuthorDetails" Height="450" Width="800">
	<DataGrid x:Name="authorDataGrid" AutoGenerateColumns="False" Columnwidth="*" x:FieldModifier="public">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Id" Binding="{Binding ID}" />
				<DataGridTextColumn Header="Firstname" Binding="{Binding FirstName}" />
				<DataGridTextColumn Header="Lastname" Binding="{Binding LastName}" />
				<DataGridTemplateColumn>
					<DataGridTemplateColumn.HeaderTemplate>
						<DataTemplate>
							<TextBlock Text="Action" />
						</DataTemplate>
					</DataGridTemplateColumn.HeaderTemplate>
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<Button x:Name="updateBtn" Content="Update" Click="updateBtn_Click" />
								<Button x:Name="deleteBtn" Content="Delete" Click="deleteBtn_Click"  />
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
	</DataGrid>
</Window>
